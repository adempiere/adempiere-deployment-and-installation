# Vault — real secrets file, never commit
group_vars/all.yml

# SSH private key — never commit; public key (.pub) is intentionally tracked
ssh_keys/adempiere_installation_key

# Ansible runtime files
*.retry
.ansible/

# Python
__pycache__/
*.pyc

# Editor temp files
*.swp
*.swo
*~

# OS
.DS_Store
